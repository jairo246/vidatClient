import { Server } from "../../constants/network";

/////////// Show ////////////
const linkShowAppointments = "schedules/requestSchedules";
export const urlShowAppointments  =  `${Server}/${linkShowAppointments}`;